<Window x:Class="DictateFlow.App.Views.PreviewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="DictateFlow — Preview"
        Background="{DynamicResource WindowBackgroundBrush}"
        Width="480"
        Height="360"
        MinWidth="340"
        MinHeight="240"
        Topmost="True"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Warning banner: shown when LLM enhancement failed and the raw transcript is offered -->
        <Border Grid.Row="0"
                Margin="0,0,0,8"
                Padding="8"
                Background="{StaticResource WarningBackgroundBrush}"
                BorderBrush="{StaticResource WarningBorderBrush}"
                BorderThickness="1"
                CornerRadius="3">
            <Border.Style>
                <Style TargetType="Border">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Warning}" Value="{x:Null}">
                            <Setter Property="Visibility" Value="Collapsed" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
            <TextBlock Foreground="{StaticResource WarningTextBrush}"
                       TextWrapping="Wrap"
                       Text="{Binding Warning}" />
        </Border>

        <!-- Editable: what Paste delivers is exactly what stands here -->
        <TextBox Grid.Row="1"
                 AcceptsReturn="True"
                 Text="{Binding Text, UpdateSourceTrigger=PropertyChanged}"
                 TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto" />

        <Expander Grid.Row="2"
                  Margin="0,8,0,0"
                  Header="Raw transcript"
                  IsExpanded="False">
            <TextBox MaxHeight="110"
                     Margin="0,4,0,0"
                     AcceptsReturn="True"
                     IsReadOnly="True"
                     Text="{Binding RawTranscript, Mode=OneWay}"
                     TextWrapping="Wrap"
                     VerticalScrollBarVisibility="Auto" />
        </Expander>

        <DockPanel Grid.Row="3" Margin="0,8,0,0">
            <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                <Button Width="88"
                        Margin="0,0,8,0"
                        Content="Paste"
                        IsDefault="True"
                        Command="{Binding PasteCommand}" />
                <Button Width="88"
                        Margin="0,0,8,0"
                        Content="Copy only"
                        Command="{Binding CopyOnlyCommand}" />
                <Button Width="88"
                        Content="Cancel"
                        IsCancel="True"
                        Command="{Binding CancelCommand}" />
            </StackPanel>
            <TextBlock VerticalAlignment="Center"
                       Foreground="{StaticResource SecondaryTextBrush}"
                       Text="{Binding StatusMessage}" />
        </DockPanel>
    </Grid>
</Window>
